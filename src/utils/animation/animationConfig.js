/**
 * Animation configuration settings
 */
export const defaultAnimationConfig = {
  scaleFactor: 1.0,
  animationDuration: 300, // Faster animation (was 500)
  letterPause: 50, // Shorter pause between letters (was 100)
  pencilScale: 0.5,
  pencilAngle: 30,
  spacing: {
    narrow: 18,
    standard: 30,
    wide: 40,
    space: 25
  }
}; 